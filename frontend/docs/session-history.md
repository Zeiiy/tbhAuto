# Historique de session

## Session History

### 2026-06-16 22:46
- Lancé l'app via skill `/run` — créé venv backend + pip install (fastapi, uvicorn, mss, opencv, pyautogui, etc.)
- npm install frontend (63 packages)
- Démarré uvicorn sur port 8000 et vite dev server sur port 5173
- Vérifié connectivité : frontend HTTP 200 et backend `/api/state` retourne l'état bot (running:false, calibrated:false)
